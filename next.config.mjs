/**
 * @type {import('next').NextConfig}
 */



const nextConfig = {
    output: 'export',
    reactStrictMode: true,
    images: {
        unoptimized: true,
    }
}

/*
const nextConfig = {
    basePath: '/stdphocea',
    output: 'export',
    reactStrictMode: true,
    images: {
        unoptimized: true,
    }
}
*/

export default nextConfig;

